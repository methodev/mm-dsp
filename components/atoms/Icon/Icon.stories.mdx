import { Title, Description, Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { select } from '@storybook/addon-knobs';
import icons from '@/assets/graphics/icons';
import { mainOf, idsOf } from '@/utils/ui-handlers';
import { kinds, sizes } from './characteristics';
import Icon from './Icon.vue';

<Meta title='Atoms/Icon' component={Icon} parameters={{ }} />

<Title of={Icon} />

> *This component is categorized as an atom.*

## Description
<Description of={Icon} />

## Stories

### Default
<Canvas>
  <Story name='Default'>
    {{
      components: { Icon },
      template: '<Icon :name="iconKnob" :kind="kindKnob" :size="sizeKnob" />',
      props: {
        iconKnob: {
          type: String,
          default: select('Icon', Object.keys(icons), 'single-neutral-circle')
        },
        kindKnob: {
          type: String,
          default: select('Kind', [undefined, ...idsOf(kinds)], undefined)
        },
        sizeKnob: {
          type: String,
          default: select('Size', idsOf(sizes), mainOf(sizes))
        }
      }
    }}
  </Story>
</Canvas>

## I/O
<ArgsTable of={Icon} />
