// ===================================================|
// Utils: UI HANDLERS

export const mainOf = (list) => Object.keys(list).find((key) => list[key].main);

export const idsOf = (list) => Object.keys(list);
